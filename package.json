{
  "name": "music_card",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "build": "esbuild api/index.js --bundle --platform=node --format=esm --outfile=dist/index.js --external:sharp --external:@maplibre/maplibre-gl-native --external:@napi-rs/*",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "patch-package"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1037.0",
    "@maplibre/maplibre-gl-native": "^6.0.0",
    "express": "^5.2.1",
    "musicard": "^3.0.1",
    "sharp": "^0.33.4",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "patch-package": "^8.0.1"
  }
}
